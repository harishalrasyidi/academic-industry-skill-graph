# Copy this file to .env.production on the deployment host.
# Do not commit .env.production.
FUSEKI_QUERY_URL=http://fuseki:3030/myDataset/query
FUSEKI_TIMEOUT_SECONDS=30
# Empty is correct when the frontend and /api are served by the same Nginx origin.
CORS_ALLOW_ORIGINS=
BACKEND_HOST_PORT=8000
